# Landing Page 多语言功能 - 快速开始

## 🎯 核心功能

✅ 支持三种语言：英文（EN）、简体中文（简）、繁体中文（繁）  
✅ 实时切换，无需刷新页面  
✅ 优化的产品化叙事，突出 x402 Agent 经济体愿景  
✅ 完整的类型安全支持  

## 🚀 快速体验

1. **启动开发服务器**
   ```bash
   cd apps/web
   npm run dev
   ```

2. **访问页面**
   ```
   http://localhost:3001
   ```

3. **切换语言**
   - 点击页面右上角的语言切换按钮
   - 选择 EN / 简 / 繁

## 📝 核心文案对比

### Hero Section

**英文：**
> Interactive Motion Manga  
> Agent Economy · Powered by AIGC

**简体中文：**
> 互动漫剧  
> Agent 经济体 · AIGC 驱动

**繁体中文：**
> 互動漫劇  
> Agent 經濟體 · AIGC 驅動

### Manifesto（核心愿景）

**英文：**
> x402 protocol is building an autonomous agent economy. As AI agents collaborate to create content, viewers earn Universal Basic Income (UBI) from the value they help create through engagement.

**简体中文：**
> x402 协议正在构建自主运转的 agent 经济体。当 AI agents 协作创造内容时，观众通过参与互动获得全民基本收入（UBI），分享他们帮助创造的价值。

**繁体中文：**
> x402 協議正在構建自主運轉的 agent 經濟體。當 AI agents 協作創造內容時，觀眾通過參與互動獲得全民基本收入（UBI），分享他們幫助創造的價值。

## 🎨 设计亮点

### 1. 产品化叙事
- ❌ 避免：直白的技术描述
- ✅ 采用：用户价值和收益导向

### 2. 叙事层次
1. **愿景层**：自主运转的 agent 经济体
2. **机制层**：AI 协作 + 链上分配
3. **价值层**：消费即创造
4. **载体层**：互动漫剧

### 3. 核心价值主张

**x402 Agent 经济体三大支柱：**

1. **自主运转的内容生产机器**
   - AI agents 协作驱动
   - 自动化收益分配
   - 链上透明可追溯

2. **全民基本收入（UBI）**
   - 观众边看边赚
   - 互动即挖矿
   - 四方分润：Agents、AI 模型、创作者、观众

3. **价值体系重构**
   - 人类参与内容消费 = 价值创造
   - 每次互动、共创、评论都获得回报
   - 互动漫剧驱动经济循环

## 💻 开发者指南

### 在组件中使用多语言

```tsx
'use client';

import { useLanguage } from '@/lib/i18n/LanguageContext';
import { getLandingContent } from '@/lib/i18n/landing';

export function MyComponent() {
  const { language } = useLanguage();
  const content = getLandingContent(language);
  
  return (
    <div>
      <h1>{content.hero.title}</h1>
      <p>{content.hero.description}</p>
      <button>{content.hero.cta.play}</button>
    </div>
  );
}
```

### 添加新的多语言内容

编辑 `lib/i18n/landing.ts`：

```typescript
export const landingContent: Record<Language, LandingContent> = {
  'en': {
    // 英文内容
    newSection: {
      title: 'New Section',
      description: 'Description in English'
    }
  },
  'zh-CN': {
    // 简体中文内容
    newSection: {
      title: '新板块',
      description: '简体中文描述'
    }
  },
  'zh-TW': {
    // 繁体中文内容
    newSection: {
      title: '新板塊',
      description: '繁體中文描述'
    }
  }
};
```

## 📁 文件结构

```
apps/web/
├── lib/
│   └── i18n/
│       ├── landing.ts              # 多语言内容定义
│       └── LanguageContext.tsx     # 语言状态管理
├── app/
│   ├── components/
│   │   └── LanguageSwitcher.tsx    # 语言切换按钮
│   ├── layout.tsx                  # 包含 LanguageProvider
│   └── page.tsx                    # 使用多语言内容
└── tsconfig.json                   # 路径别名配置
```

## 🔍 关键文件说明

- **landing.ts**: 所有页面文案的三语版本
- **LanguageContext.tsx**: 全局语言状态管理
- **LanguageSwitcher.tsx**: 语言切换 UI 组件
- **page.tsx**: Landing Page 主页面（已改为 'use client'）

## ✨ 特性

- ✅ 类型安全的多语言内容
- ✅ React Context 全局状态管理
- ✅ 实时切换，无需刷新
- ✅ 优雅的视觉设计
- ✅ 易于扩展和维护

## 📚 更多文档

- `LANDING_PAGE_I18N.md` - 详细的多语言实现说明
- `OPTIMIZATION_SUMMARY.md` - 完整的优化总结

---

**开发环境：** http://localhost:3001  
**默认语言：** 英文（EN）  
**支持语言：** EN / 简 / 繁

